apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.apiService.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.apiService.serviceAccount.roleArn }}
